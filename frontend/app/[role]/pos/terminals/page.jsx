import Terminals from "@/components/shared-components/pos/terminals/terminals";

export const metadata = {
  title: 'Terminal Management | Supermarket POS',
  description: 'Manage POS register hardware and active sessions.',
};

export default function TerminalManagementPage() {
  return <Terminals />;
}
